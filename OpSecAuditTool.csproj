<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <!-- Mehrgrößen-ICO für die Windows-EXE; die Fenster selbst verwenden das
         plattformübergreifende PNG-Asset. -->
    <ApplicationIcon>Assets\AppIcon.ico</ApplicationIcon>
    <!-- Öffentliche Projektmetadaten verwenden bewusst das Entwicklerpseudonym. -->
    <Authors>SolidStateNetwork</Authors>
    <Product>OpSec Audit Tool</Product>
    <Description>Portable OpSec-, Datenschutz- und Systemhärtungsprüfung für Linux und Windows.</Description>
    <Copyright>Copyright © 2026 SolidStateNetwork</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="12.1.0" />
    <PackageReference Include="Avalonia.Desktop" Version="12.1.0" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.1.0" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="12.1.0" />
    <PackageReference Include="AvaloniaUI.DiagnosticsSupport" Version="2.2.3">
      <IncludeAssets Condition="'$(Configuration)' != 'Debug'">None</IncludeAssets>
      <PrivateAssets Condition="'$(Configuration)' != 'Debug'">All</PrivateAssets>
    </PackageReference>
    <!-- Nur tatsächlich verwendete Bilder in das Anwendungspaket aufnehmen. -->
    <AvaloniaResource Include="Assets\Profile.png" />
    <AvaloniaResource Include="Assets\AppIcon.png" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
  </ItemGroup>
</Project>
